# walrus-pchannel

Channel abstraction for the Walrus platform.

Provides the `Channel` trait, `Platform` enum, `ChannelMessage`, and
`ChannelRouter` for routing messages across multiple communication platforms.

## License

GPL-3.0
