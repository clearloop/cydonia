# walrus-mcp

MCP server bridge for Walrus agents.

Provides `McpHandler` (implements `Hook`) and `McpBridge` for connecting
agents to external MCP servers via the rmcp SDK, with dynamic add/remove/reload.

## License

GPL-3.0
